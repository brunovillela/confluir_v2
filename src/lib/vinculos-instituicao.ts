/**
 * Vínculo da pessoa com a entidade (`usuarios.vinculo_instituicao`). Quem tem
 * um destes entra no quadro: listas do Pessoal, aniversários do painel, caixa
 * e onboarding em lote. Sem vínculo, a pessoa só tem o acesso que receber.
 */
export const VINCULOS_INSTITUICAO = [
  "Funcionário(a)",
  "Diretor(a)",
  "Prestador(a) de serviço",
] as const
